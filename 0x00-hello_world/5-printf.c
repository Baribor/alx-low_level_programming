#include <stdio.h>

/**
 * main - The entry point
 *
 * Description: Prints to std output using printf
 * and ending with a newline char.
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");

return (0);
}
